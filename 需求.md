需求梳理:
- 替换服饰中的logo
- 替换服饰中logo的颜色
- 在指定位置中加logo
- 替换服饰的面料材质（整体/局部）

我预期是调用中转站的gemini 3 image api来实现。
替换logo用户使用流程：
- 上传一张或多张产品图片
- 上传一张或多张将要替换的logo图片
- 用户手动圈选所有产品图中的logo区域
- 并发调用生图api进行生图
- 生图逻辑：图1是产品原图，图2是用户圈选logo后的图片，图3是logo图片，将此三张图片发送给生图api，并附带提示词“图1是产品原图，图2是用户圈选logo后的图片，图3是logo图片。
我的目标是
- 按照用户圈选的位置，将原图中的logo替换为（3）
- （3）大小不能大于原logo大小”
- 前端展示所有产品图、logo图、圈选后的图片的关系列表，一起的就放在一行，到时候结果图也放在一行，生图提示词也放在一行，提供一个批量填充所有所有提示词输入框的输入框，填写的就是咱们提供的默认提示词。
- 至此，我们的系统就批量的将产品图×logo图发送给生图api，批量生图（图片数量为：所有产品图×所有logo图）
- 当生图成功后，我们将所有的结果图都展现给用户。每张结果图，都要保留原图片、圈选后的图片、logo、结果图片的关联映射。用户选择哪张结果图，系统就知道对应哪些素材图（原图片、圈选后的图片、logo），便于单张、批量重试生图
- 可能会出现某个图片或者所有图片都生成效果不及预期的情况，我们要允许批量重新生成，或单张生成。
- 批量重新生成与单张：允许用户全选结果图或自由单选或多选结果图，提供提示词输入框，默认值为上文提到的，允许用户自由修改提示词，并点击重新生成按钮，按照已经选择的图片进行批量重新生成。
- 前端页面预期效果：
  - 允许上传100张商品图片、100张logo图片
  - 上传图片后，在前端页面根据乘积的方式(一个商品对应所有logo图片)，列表的形式展现出来，第一个列表项为商品图片，第二个列表项为logo图片，第三个为圈选后的图片，第四个为提示词输入框，第五个为重新生成按钮
  - 备注：我们假设所有商品图需要圈选的地方都一样，所以，用户只需要圈选一张图片，即可完成所有商品图的圈选。同时，我们允许用户为单独的商品图进行圈选，以生成专属的圈选后的图片。

#  细节问题
- √p0点击任意图片都应该可以查看大图，但是当前不能，需要修复bug
- √p0修改单张圈选后的图片，竟然改变了所有圈选图
- √P0如果图片数量骤增，比如用户上传了100张logo图片，100张产品图片，如何保证前端页面不会崩溃？
- √P0当前生图是如何并发的？并发了是多少？——100
- √p0添加一键下载所有结果图功能
- √p0给任务列表添加反选功能
- √P0生成图片时控制台日志偏多，一百张图片并发时可能导致性能问题
- √p0允许用户不选择纵横比、分辨率，后端不填写这些参数

调试出首图或尾图作为分辨率参考


- p1需要持久化保存所有任务记录及图片数据，数据索引保存到数据库，图片文件保存到项目中指定文件夹下
- p1圈选功能不易用，无法使用Ctrl+Z或Ctrl+y快捷键，无法放大图片精细化圈选，无法闭合圈选（当末尾的点点击第一个点时，应该需要自动闭合，而不是只能依赖“闭合”按钮）

- p2如果产品图片以红色为主，那么系统的圈选图片就看不清颜色了，我建议提供一个按钮给圈选图功能，如果用户勾选，则圈选颜色变为绿色，这样就不担心产品颜色问题了
