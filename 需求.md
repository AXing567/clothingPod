需求梳理:
- 替换服饰中的logo
- 替换服饰中logo的颜色
- 在指定位置中加logo
- 替换服饰的面料材质（整体/局部）

我预期是调用中转站的gemini 3 image api来实现。
替换logo用户使用流程：
- 上传一张或多张产品图片
- 上传一张或多张将要替换的logo图片
- 用户手动圈选所有产品图中的logo区域
- 并发调用生图api进行生图
- 生图逻辑：图1是产品原图，图2是用户圈选logo后的图片，图3是logo图片，将此三张图片发送给生图api，并附带提示词“图1是产品原图，图2是用户圈选logo后的图片，图3是logo图片。
我的目标是
- 按照用户圈选的位置，将原图中的logo替换为（3）
- （3）大小不能大于原logo大小”
- 前端展示所有产品图、logo图、圈选后的图片的关系列表，一起的就放在一行，到时候结果图也放在一行，生图提示词也放在一行，提供一个批量填充所有所有提示词输入框的输入框，填写的就是咱们提供的默认提示词。
- 至此，我们的系统就批量的将产品图×logo图发送给生图api，批量生图（图片数量为：所有产品图×所有logo图）
- 当生图成功后，我们将所有的结果图都展现给用户。每张结果图，都要保留原图片、圈选后的图片、logo、结果图片的关联映射。用户选择哪张结果图，系统就知道对应哪些素材图（原图片、圈选后的图片、logo），便于单张、批量重试生图
- 可能会出现某个图片或者所有图片都生成效果不及预期的情况，我们要允许批量重新生成，或单张生成。
- 批量重新生成与单张：允许用户全选结果图或自由单选或多选结果图，提供提示词输入框，默认值为上文提到的，允许用户自由修改提示词，并点击重新生成按钮，按照已经选择的图片进行批量重新生成。